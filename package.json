{
  "name": "pc-cafe-qa-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix app && npm install --prefix mock-api",
    "dev:api": "npm run dev --prefix mock-api",
    "dev:app": "npm run dev --prefix app",
    "test": "playwright test",
    "test:e2e": "playwright test tests/e2e",
    "test:api": "playwright test tests/api",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^25.9.1",
    "typescript": "^5.9.2"
  }
}
